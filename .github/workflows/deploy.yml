name: Deploy to Heroku

on:
  push:
    branches:
      - master  # O "main" si es tu rama principal

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install Heroku CLI
        run: |
          curl https://cli-assets.heroku.com/install.sh | sh

      - name: Set Git User  # Configurar nombre y correo para Git
        run: |
          git config --global user.email "2220211035@estudiantesunibague.edu.com"  # Reemplaza con tu email
          git config --global user.name "mmillan18"  # Reemplaza con tu nombre

      - name: Authenticate with Heroku
        run: heroku auth:token
        env:
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}

      - name: Build and Deploy to Heroku
        env:
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
        run: |
          git init
          heroku git:remote -a ${{ secrets.HEROKU_APP_NAME }}
          git add .
          git commit -m "Deploying to Heroku"
          git push -f heroku master
